# STRUCTURE — 저장소 공통 구조 규칙

모든 저장소(stock, sap, capcut, general, …)에 동일하게 적용하는 **2단 구조** 표준.
기존 저장소를 한꺼번에 갈아엎지 않는다. **새로 추가되는 것부터** 이 규칙을 적용하고, 기존 콘텐츠는 여유가 될 때 같은 규칙으로 맞춰간다.

## 1. 계층은 2단까지

```
<repo>/                   ← 1단: 대분류(category). 저장소 = 하나의 과목/주제
├─ README.md              ← 저장소 개요 + Session 인덱스(진행표)
├─ STRUCTURE.md           ← 이 문서
├─ SessionNN_주제/         ← 2단: 중분류. 한 덩어리로 학습·집필하는 단위(폴더)
│   ├─ README.md          ← (선택) 세션 개요·학습순서
│   ├─ 01_....md          ← 콘텐츠 파일
│   ├─ 02_....md
│   └─ ...
└─ Session(NN+1)_.../
```

- **1단 = repo** : GitHub 저장소 자체가 대분류다. (예: `stock` = 주식 분석)
- **2단 = Session 폴더** : 중분류. `SessionNN_주제` 형식.
- **콘텐츠는 파일** : 세션 폴더 안의 `.md`. **세션 아래에 다시 하위 폴더를 만들지 않는다.**

## 2. 네이밍 규칙

| 대상 | 형식 | 예시 |
| --- | --- | --- |
| Session 폴더 | `SessionNN_주제` (NN = 2자리) | `Session01_밸류에이션` |
| 콘텐츠 파일 | `NN_주제.md` (NN = 2자리, 세션 내 순서) | `03_PER.md` |
| 번호 | 항상 2자리(01, 02, …)로 정렬 보장 | — |

## 3. 각 저장소 README 필수 항목
- 저장소 한 줄 설명(대분류가 무엇인지)
- **Session 인덱스 표** (Session 번호 / 주제 / 핵심 파일)
- 진행 상태 체크리스트

## 4. 커밋 메시지 규칙
- 세션 태그 접두어를 붙인다: `[SessionNN] 내용`
  - 예) `[Session01] EPS 초고`, `[Session03] 수급 분석 보강`
- 저장소 구조 자체를 바꾸는 커밋은 `[chore]` 또는 `[structure]` 접두어.

## 5. 확장 방침
- 새 중분류가 생기면 새 `SessionNN_주제/` 폴더를 추가한다.
- 한 세션이 너무 커지면 세션을 **분리**하되, 하위 폴더로 깊이를 늘리지 않는다. (2단 유지)
- 완성된 원고만 커밋한다. 미완성분을 완료로 표기하지 않는다.
